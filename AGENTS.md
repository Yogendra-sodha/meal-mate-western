> [!IMPORTANT]
> Avoid rewriting published git history — force pushing, or rebasing/amending/
> squashing commits that are already pushed — so the project history stays
> intact for everyone working on it.
>
> Commits pushed to `main` deploy automatically to Vercel, so keep the branch
> in a working state.
